package org.usfirst.frc.team3555.robot.subsystems;

public interface SubSystem {
	/*
	 * Since every subsystem needs to update every time the loop goes through
	 * this interface standardizes the use of the update method in all subsystems
	 */
	void update();
}
